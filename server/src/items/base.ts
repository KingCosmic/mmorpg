import { Item, ItemType } from "../types";

/**
 * These are the items every player gets when they first make an account.
 */
const baseItems:Item[] = [
  {
    id: '',
    name: 'stone pickaxe',
    icon: '',
    type: ItemType.PICKAXE
  }
]

export default baseItems